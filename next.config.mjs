/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',

    // Disable Image Optimization
    images: {
      unoptimized: true,
    },
    

  // Disable Image Optimization
 
};

export default nextConfig;
